# DDR PS2 Controller
#
# Switch Component
#
# Kevin Cuzner

include ../common/common-top.mak

PROJECT=ddr-ps2-switch

all:: $(BINDIR)/$(PROJECT).bin $(BINDIR)/$(PROJECT).hex $(BINDIR)/$(BINARY).bin $(BINDIR)/$(BINARY).hex

include ../common/common-bot.mak

